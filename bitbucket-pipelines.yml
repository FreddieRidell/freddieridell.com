image: codogo/pipelines-universal:latest

pipelines:
  branches:
    master:
      - step:
          script:
          - yarn install --pure-lockfile
          #- ./generateFavicons.sh
          - yarn run format
          - yarn run build
          - aws s3 sync public s3://freddieridell.com
